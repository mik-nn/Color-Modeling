# TODO

- [ ] Fix merge conflict markers in `frontend/package.json` (broken JSON).
- [ ] Ensure `frontend/package.json` has consistent `scripts` and consistent `vite`/`vitest` versions.
- [ ] Run `cd frontend && npm install`.
- [ ] Run `cd frontend && npm test`.
- [ ] Run `cd frontend && npm run build`.
- [ ] Run `cd frontend && npm run dev` (optional smoke test).

