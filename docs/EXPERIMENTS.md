# EXPERIMENTS.md — Append-only experiment log

> Each row is one experiment. Newest at the top. Never edit a past row — append a new
> row when a result is superseded or corrected.

| 2026-06-12 | H18 — ink-coverage correlation + high-CMY anchor augmentation on DecorMatte→ChromataWhite (H18a+H18c CONFIRMED; H18b rejected) | `BC_DecorMatte_P9000_mk_CanvasMatte` → `BC_ChromataWhite_P9000_mk_CanvasMatte` (1 pair). `scripts/experiments/h18_ink_coverage.ts`. D1 rank=5 uvBandCount=4 D7 OBA. S1 k=13 baseline + 3 high-CMY augmentation anchors (nearest grid patches to R=40,G=0,B=100/150/190). 892 non-anchor baseline patches. | — | **H18a** Spearman(ink, ΔE00) = **0.712** (gate >0.50) → PASS. **H18b** Spearman(ink, 530–580nm err) = **−0.120** → REJECT (negative; the 530–580nm elevation in H17 P95 patches was a consequence of their gamut-sector location, not a direct coverage→green-yellow-band effect). **H18c** Augmented S1+3 high-CMY (k=16): P95 6.419 → **4.787** (Δ=1.632, gate ≥1.0) → PASS; median 1.886 → 1.817. High-CMY anchors: (31,0,95), (31,0,159), (31,0,191). Coverage bucket analysis: ink<256 P95=2.1, ink 256–383 P95=3.4, ink 384–511 P95=6.1, ink ≥512 P95=7.7 — clear monotonic escalation. After augmentation worst patches shift from dark blue-violet to heavy-Y olive-green sector (R≈100–160, G≈85–170, B=0). | **H18a+H18c confirmed; H18b rejected.** Total ink coverage is a strong systematic predictor of D1 transfer error (Spearman 0.712): error scales monotonically from P95=2.1 at light tones to P95=7.7 at maximum coverage. The 530–580nm band is NOT independently coverage-driven — H17's green-yellow elevation was a statistical artifact of which patches land in the P95 tier. Augmenting S1 with 3 blue-violet-sector anchors fixes that gamut sector (P95 drop 1.63 ΔE00), but the new worst sector becomes heavy-Y patches — the same ink-coverage root cause, different gamut location. The D1 paper-ratio rank-5 residual model is fundamentally limited at high total ink density regardless of gamut sector. | H19: test whether adding 3 more anchors in the heavy-Y sector (R≈100–160, G≈100–170, B≈0) further reduces P95. Alternatively: evaluate whether raising residualRank from 5 to 8 (covering p95 rank from SVD analysis) closes the high-ink gap without more anchors. |

| 2026-06-12 | H17 — spectral residual band analysis on DecorMatte→ChromataWhite (REJECTED as stated; unexpected finding) | `BC_DecorMatte_P9000_mk_CanvasMatte` → `BC_ChromataWhite_P9000_mk_CanvasMatte` (1 pair). `scripts/experiments/h17_residual_bands.ts`. D1 rank=5 uvBandCount=4 D7 OBA, S1 k=13. 892 non-anchor patches evaluated. OBA mismatch: DecorMatte=1.196, ChromataWhite=1.017, delta=0.179. | — | **D1 baseline:** median=1.886, P95=6.419. **UV/VIS ratio in P95 group: 0.933** (gate was > 2.0). Per-band mean |err| for P95 patches: UV 380–430 nm = 0.0150 (0.93× avg — BELOW average), VIS 530–580 nm = 0.0205–0.0234 (1.28–1.56× avg — ELEVATED). Top-5 bands by P95-group error: 570 nm, 580 nm, 540 nm, 560 nm, 380 nm. Worst 10 patches: R=31–63, G=0–28, B=63–191 (dark blue/violet gamut boundary). | **H17 REJECTED as stated (OBA dominance hypothesis wrong).** Unexpected finding: D7 OBA correction is working — UV error in P95 patches is *below* dataset average. The P95 error is concentrated in the visible green-yellow band (530–580 nm) for dark blue-violet gamut-boundary patches (heavy C+M coverage). Root cause: ChromataWhite (bright-coated) vs DecorMatte (natural matte) have different ink-substrate interaction at high C+M density; D1's paper-ratio + rank-5 residual cannot model this nonlinear difference. The OBA-targeted anchor strategies (D-optimal UV-SVD, H13c M2 anchors) would not help. | H18: check whether gamut-boundary error at 530–580 nm correlates with total ink coverage (C+M+Y device sum). Test whether adding 2–3 high-CMY-density chromatic anchors (R≈40, G≈0, B≈100–200) to S1 closes the gap. |

| 2026-06-12 | H16 — per-substrate YN exponent at 640–680 nm (REJECTED) | 26 BC ICM profiles (CanvasMatte n=20 pairs, WCRW n=56, CanvasSatin n=12, etc.). `scripts/experiments/h16_redband_yn.ts`. Baseline: D1 S1 rank=5 uvBandCount=4 D7 OBA. H16: fit n_B(λ) at 640–680nm from B's cyan ramp (4 patches: RGB (0,255,255), (64,255,255), (128,255,255), (192,255,255)) via golden-section search; override D1 at those bands. | — | **H16 v1 (unmasked, all patches):** CanvasMatte Δmed=+1.770, ΔP95=+14.018 — catastrophic. **H16 v2 (cyan-mask G≥220 B≥220):** CanvasMatte Δmed=+0.002, ΔP95=0.000 — null effect. Same on all other presets. n_B range 1.72–2.60 (physically plausible). 0/6 worst pairs improved. | **H16 REJECTED.** Two failure modes: (1) Unmasked YN: single-ink model (paper↔cyan) massively overpredicts reflectance at 640–680nm for M/Y-laden patches (magenta absorbs heavily there; YN model predicts ≈paper-white → +14 ΔE00 P95). (2) Cyan-masked YN: fires only in G≥220 B≥220 sector, which D1 already handles well (Δ≈0). Root cause of Canvas Matte P95=2.2: DecorMatte↔OBA-extreme pairs (ChromataWhite, 800M, BelgianLinen) — a whole-spectrum OBA-mismatch problem, not a 640–680nm cyan-optics issue. H16 hypothesis was looking at the wrong mechanism. | Diagnose OBA-extreme pair residuals spectrally: which bands drive P95 for DecorMatte→ChromataWhite? Candidate: H13c adaptive gate already gave −0.08 ΔE on these pairs; the remaining gap may require M0/M2 measured OBA at anchors (needs files with M2). Alternative: D-optimal anchor selection targeting OBA-band SVD components. |

| 2026-06-12 | H4 batch re-run after `alignProfiles` refactor + paper detection by RGB(255,255,255) | 27 BC ICM profiles (same as previous run). `h4_batch.ts` updated to use `alignProfiles` instead of deleted `alignByCommonSampleIds`; paper row found by exact RGB(255,255,255) device-value lookup. | — | **same-mode (98 pairs):** med-of-meds=0.837, H4 pass=80.6% — **identical** to pre-refactor run. **cross-mode (502 pairs):** med-of-meds=2.468, pass=0.8% — identical. CAE_D7 same-mode k=0: med-of-meds=1.169, pass=54.1%. | Refactor is semantically correct: alignProfiles (exact-match path for same-chart) + RGB(255,255,255) lookup reproduce identical results. H4-revised (same-mode ≥80% at k=13) confirmed stable. h4_batch.ts is now unblocked for future runs after any predictor change. | Extend h4_batch with CAE_D7 k=13 (H10b) column for same-mode pairs to validate H10b at batch scale. |

| 2026-06-12 | CGATS cross-grid alignment bug — SAMPLE_ID by device value; paper detection by exact RGB(255,255,255) | USFA (MOAB, ~1550 patches) paired against BC Bright profiles (2033 patches) via TransferView. | Session fix (no separate commit yet). | **Before:** A3=23.87, D1=30.87 ΔE00, R²=−371 to −393, OBA score=4.548. **After fix:** expect ≤ 2 ΔE00 on same-mode USFA pairs. Root cause confirmed: CGATS row 1727 in Bright=RGB(255,0,255) magenta; row 1727 in USFA Textured=RGB(255,255,255) paper. Sequential `P{row}` IDs matched them as same patch. | **Fix 1 (upstream):** `cgatsParser.ts` generates `SAMPLE_ID = RGB_{R}_{G}_{B}` when field absent. **Fix 2 (downstream):** `TransferView.tsx` finds paper white by explicit `R=G=B=255` device-value lookup, not by position or brightness. **Also found:** `evaluate_d7.json`, `evaluate_d7_USFA.json`, `evaluate_d7_PremiumLuster_test.json` are identical (n=6, med=1.306, p95=3.630) — copy-paste bug in evaluation script; those files do not reflect actual per-mode weights. | Re-run USFA Playwright test with MOAB profiles to verify fix. Re-generate evaluate_d7_USFA.json and evaluate_d7_PremiumLuster_test.json from correct weights. Run H4 batch runner after CGATS fix to get clean aggregate stats. |

| 2026-06-09 | Per-mode CAE_D7 retraining — PremiumLuster (4 MOAB), CanvasMatte (5 BC+1 MOAB Anasazi), CanvasSatin (4 BC pk) | PremiumLuster: 4 MOAB profiles (Colorado Fiber Gloss/Satine, Juniper Baryta, Lasal Exh Luster) + 1 BC validation (VibranceLuster PLPP260). CanvasMatte: 5 BC mk + 1 MOAB Anasazi Canvas (Exh Canvas Matte preset), BC_Lyve as test, Anasazi as cross-vendor validation. CanvasSatin: 4 BC pk profiles, BC_Silverada as test. All trained with cv_train.py --folds 2 --epochs 50 --export-suffix MODE. | Old PremiumLuster (2 BC, 2026-06-08): CAE_LOO med 7.77–8.71 ΔE (far above C7/D1 ~1.4). Old CanvasMatte (5 BC, May 30): median-of-medians 1.73 ΔE for BC_800M target. | **PremiumLuster (a13 test):** Juniper Baryta (baryta vs cotton): med 0.78–0.88 ΔE00, p95 3.22–4.50. Cross-vendor BC_VibranceLuster (val): med 2.54–2.65, p95 5.24–5.49. **CanvasMatte (a13):** BC_Lyve (test): med 0.74–0.81, p95 1.92–2.76. MOAB Anasazi (cross-vendor val): med 1.39–1.52, p95 3.20–3.72. **CanvasSatin (a13):** BC_Silverada (test): med 1.38–1.56, p95 3.92–4.34. | MOAB профили в pool радикально улучшают PremiumLuster: модель теперь знает baryta substrate (Juniper) → med 0.78–0.88 vs old CAE_LOO 7.77 (×10 улучшение). CanvasMatte с Anasazi: cross-vendor BC→MOAB transfer med ~1.4 (приемлемо). CanvasSatin: Silverada (металлическая текстура) med 1.38–1.56 — хорошо для pk-only режима. Все три режима: N=2027 общая сетка (MOAB CGATS densest, IDW-интерполяция BC patches onto this grid). | Re-run H14 CAE_LOO experiments on updated PremiumLuster bundle (was 7.77 → expect ≤ 2.0 with 4 MOAB in training). Проверить: нужна ли CanvasSatin модель в UI? Обновить WCRW split.json (currently no explicit split file). |

| 2026-06-09 | USFA CAE_D7 retraining — корректный per-mode split (Kozo в train, Unryu в validation) | 7 MOAB USFA P9000 профилей (Epson USFA preset). `python/cae/cv_train.py --variant d7 --folds 5 --epochs 50`. Pool=6 (train∪test), val=Unryu. Финальная модель обучена на всех 6, включая Kozo (washi-якорь). Диагностика спектральных расстояний: Unryu dist-to-centroid=32.9 (outlier), Kozo=20.7 (ближайший washi). Старый split.json имел PremLuster имена → auto-split не гарантировал Kozo в train. | Baseline USFA a13: Unryu med-of-med=3.64 ΔE00, p95 до 18.2 (per `evaluate_d7_USFA_a13.json`, до исправления). | **Новые результаты (a0, per `evaluate_d7.json`):** Kozo→Unryu med=**1.18**, max med=1.38, все refs ≤1.49 ΔE00, p95 ≤4.0. **(a13):** med=1.21–1.49. Улучшение: 3.64→1.30 (×2.8). Также: добавлен CGATS-путь в `icm_reader.py` (BC CxF3-core + MOAB targ/CGATS), `profiles-all.json` = 44 профиля (27 CxF + 17 CGATS, без Slickrock Silver metallic). | **Kozo в train = обязательное условие.** Без washi-опоры substrate latent не охватывает washi-субпространство. С явным split (5 train включая Kozo + 1 test + 1 val=Unryu) + cv_train на полном pool=6 → Unryu 1.18–1.38 ΔE00 вместо 3.64. Корень провала: auto-split по seed=42 не гарантировал нужный состав обучения. **icm_reader.py** починен: оба CxF namespace (cc: старый + CxF3-core BC профили) + CGATS targ-путь для MOAB — 44/45 профилей читаются. | Запустить per-mode обучение для остальных режимов (WCRW, CanvasMatte, PremLuster) с явными splits. Исследовать p95≤4 для Unryu: какие патчи outlier (высоконасыщенные или тёмные washi-текстурные области)? |

| 2026-06-08 | Cross-mode SVD tier analysis — per-(ref,tgt) preset breakdown, coverage vs k, difficulty tiers | 27 BC P9000 profiles, 558 cross-mode directed pairs. `python/cae/rank_analysis.py --variant raw` + ad-hoc analysis script reading cached `rank_analysis_raw.npz`. | — | **Coverage (% pairs where k SVs ≥ 99 % energy):** k=5: 19.7 %, k=6: 59.5 %, k=7: **91.8 %**, k=8: **99.3 %**, k=10: 100 %. **Tier breakdown:** easy (rank≤5): 110 pairs (19.7 %), RMS=0.050; medium (rank 6–7): 402 pairs (72 %), RMS=0.038; hard (rank≥8): 46 pairs (8.2 %), RMS=0.030. **Hardest target presets:** BC_1930_pk_EMP, BC_ArtPeelBlckt_mk_EMP, BC_VibranceGloss_pk_PGPP, BC_VibranceLuster_PLPP260 — all median rank=7, p95=8. Easiest: BC_VibranceMetallic_PGPP260 median rank=5 (near-Luster spectrally). Notable: hard pairs have lower RMS (0.030) than easy (0.050) — compact but structurally complex. Hardest single pairs (rank=8): ArtPeelBlckt_EMP↔1930_EMP, VibranceLuster↔ArtPeelBlckt_EMP (RMS≈0.03). | **Для cross-mode k=8 покрывает 99.3 % пар.** Hard-tier (EMP-профили) требует k=9. Структурная сложность не коррелирует с амплитудой остатка — EMP-пары «тихие» но многоуровневые. D-optimal выбор якорей по SVD-базису остатка позволяет приближаться к теоретическому пределу rank+1 вместо greedy S1=13. Текущий S1=13 перекрывает 100 % и является безопасным. | Реализовать D-optimal anchor selection (оффлайн SVD пары → QR-pivot на basis матрице → выбрать k строк максимального объёма). Сравнить D-optimal k=8 vs greedy k=13 на cross-mode парах в k-sweep эксперименте. |
| 2026-06-08 | SVD rank analysis — raw vs d7 OBA-cleaned, all 27 profiles (702 directed pairs), same-mode vs cross-mode | 27 BC P9000 profiles parsed via `exportCaeData.ts` (extended: `CAE_INK_MODE=all`, `CAE_OUT_FILE`) → `profiles-all.json` (8.92 MB, 25 080 patches). `python/cae/rank_analysis.py` — per-pair SVD of `D = X_B − X_A` (N×36), effective rank at 99% Frobenius energy. Variants: raw spectra and d7 (OBA-cleaned via `oba.py`). Results cached in `weights/rank_analysis_{raw,d7}.npz`. | — | **same-mode** (n=144): raw median rank@99% = **5**, p95 = 8, RMS(D) = 0.0328. d7: median = **5**, p95 = 8, RMS(D) = 0.0319. **Cross-mode** (n=558): raw median = **6**, p95 = 8, RMS(D) = 0.0381. d7: median = **6**, p95 = 8, RMS(D) = 0.0376. Raw ≡ d7 in rank; RMS drops ~2% after OBA removal. Cumulative energy: 5 SVs capture 99.0% same-mode / 98.2% cross-mode; 8 SVs: 99.8% / 99.7%. Cross-mode adds exactly 1 rank unit vs same-mode. | **OBA-компенсация не снижает ранг остаточного поля.** OBA — малая поправка к амплитуде (2% RMS), не отдельная структурная степень свободы. Теоретический минимум k (info-theoretic): rank+1 = **6 патчей same-mode, 7 cross-mode**. D-оптимальный выбор точек по базису остатков позволит подойти к этому пределу. Текущий S1=13 избыточен (перекрывает p95 ранга = 8 с запасом). **Цепочка: low-rank completion + D-optimal anchors — правильный путь к min-k.** CAE не является решением: провал вызван структурным несоответствием, а не нехваткой мощности. | Реализовать k-sweep с D-оптимальным выбором якорей по базису остатков (offline SVD → per-pair D-optimal) vs текущий greedy worst-patch. Построить кривую pass-fraction vs k для D1/C7 same-mode и cross-mode. |
| 2026-06-08 | Фаза C v3 — H14 CAE_LOO on PremiumLuster: 6 profiles (2 BC + 4 MOAB via WLS interpolation) | 2 BC + 4 MOAB PremiumLuster P9000. MOAB profiles aligned to BC target grid via WLS RGB interpolation (k=16, power=2). Both pair directions: VL→RS and RS→VL. CAE_D7 per-mode PremiumLuster bundle used. | (pending) | **VL→RS**: D1 1.62/6.63, C7 1.37/4.90, CAE_D7 8.13/19.02, **CAE_LOO 7.77/19.10** (LOO support=5). **RS→VL**: D1 1.62/5.66, C7 1.38/7.34, CAE_D7 9.52/18.67, **CAE_LOO 8.71/18.58** (LOO support=5). | WLS interpolation of MOAB profiles to BC target grid works correctly (5 support profiles). However gain from 1→5 support is marginal (7.81→7.77 / 8.89→8.71). Root cause: PremiumLuster CAE bundle was trained on only 2 BC profiles → substrate latent manifold too narrow; adding cross-vendor MOAB support barely helps because CAE hasn't seen MOAB substrate diversity during training. CAE_LOO still 5–6 ΔE above C7/D1. **Conclusion**: H14 algorithm is complete and correct; WLS cross-grid interpolation for MOAB support works; the PremiumLuster failure is a training-data limitation (2 BC training profiles → undertrained per-mode bundle), not an algorithmic one. | Retrain PremiumLuster CAE bundle including MOAB profiles (6 profiles total, 4-2 LOO split possible). Expected: substrate manifold richer → LOO latent search more effective → approach C7/D1 parity as with WCRW. |
| 2026-06-08 | Фаза C — H14 CAE_LOO on PremiumLuster (k=3 few-shot, all 905 support patches) | 2 PremiumLuster BC P9000: RiverStoneSatinRag + VibranceLuster. Both pair directions: VL→RS and RS→VL. Predictors: D1+S1(k=13), C7+S1(k=13), CAE_D7(k=13), CAE_LOO(k=3 few-shot / all 905 support). CAE_D7 per-mode PremiumLuster bundle used. | (pending) | **VL→RS**: D1 1.62/6.63, C7 1.37/4.90, CAE_D7 8.13/19.02, **CAE_LOO 7.81/19.28**. **RS→VL**: D1 1.62/5.66, C7 1.38/7.34, CAE_D7 9.52/18.67, **CAE_LOO 8.89/18.45**. LOO support count = 1 (only 1 other same-mode profile available). | CAE_LOO correctly outperforms static CAE_D7 (−4 % to −7 % median) confirming the algorithm is functional. However, CAE_LOO with only 1 support profile is far above C7/D1 (7.8–8.9 vs 1.4). Root cause: PremiumLuster dataset has only 2 profiles total → 1 support profile is insufficient for effective Nelder-Mead substrate latent search. Contrast: WCRW with 4 profiles (3 support) achieves CAE_LOO median 1.26 (k=13, previous session). **H14 conditional pass**: algorithm is correct; requires ≥3 same-mode support profiles. Bug corrected: LOO was previously passing S1 k=13 as few-shot signal (over-specifying target info). Fixed to k=3 (paper + 2 chromatic) + all 905 support patches per profile. | (a) Load more PremiumLuster profiles (acquire additional BC/MOAB PLPP data) to reach ≥3 support; (b) Re-run WCRW with corrected k=3 to get clean H14 metrics; (c) Update H14 status in RESEARCH_HYPOTHESIS.md. |
| 2026-06-08 | H14 baseline — CAE static vs C7/D1 across all modes; LOO not yet wired | All 27 BC P9000 profiles. Manual UI comparison (ref/target same-mode pairs): WCRW, PremiumLuster, CanvasMatte. Predictors: C7+S1, D1+S1 rank5, CAE_RAW k=0, CAE_D7 k=0 (per-mode bundle where available). | (pending) | **WCRW** (per-mode bundle available): CAE_D7 median ~1.0–1.1, competitive with C7/D1. **PremiumLuster** (per-mode bundle available): CAE_D7 median 2.5–4.0, C7 1.1–1.4, D1 1.1–1.5. **CanvasMatte**: CAE_D7 median 2.5–4.5, C7/D1 1.2–1.7. Pattern: C7 and D1 consistently better than all CAE variants on every mode except WCRW. | Static CAE fails on held-out substrates: H14 confirms the asymmetry — anchor-fit predictors (C7, D1) adapt per pair at inference; static CAE has no adaptation mechanism for a new (held-out) substrate. `predictTargetWithLOO` exists in `lib/analyzers/dynamicLOO.ts` but was never imported into TransferView — H14 implementation is dead code. Bugs identified in current LOO: (1) `refProfile: 'support'` → null_id used for ink encoder instead of real support substrate; (2) init latent = zeros not target paper encoding; (3) metrics include anchors. | Implement H14 fully: (a) export CAEForward from cae.ts; (b) rewrite dynamicLOO.ts with correct profile IDs, init from target paper, fast subsampled loss, few-shot anchor patches; (c) wire `CAE_LOO` into TransferView with same-mode support set construction; (d) evaluate on PremiumLuster and compare to C7/D1. |
| Date       | Experiment                                                                   | Profiles                                                                                                                                                                                                                                                                                                                                                                                 | Commit    | Key metric                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Conclusion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Next step                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-30 | Per-mode CAE_D7 + H10b anchor fine-tune                                       | Three Epson presets re-exported and trained separately on the existing 3-set + 5-fold CV pipeline (`split.py` floor lowered to 5; `cv_train.py` skips eval-< 2 folds and falls back to test for monitoring if validation is < 2). Per-mode 3-fold CV + final train on train ∪ test, 80 epochs. H10b evaluated via `evaluate.py --anchors 13` (Adam fine-tune on `substrate_latent_B`, 200 steps, lr = 0.05); ΔE measured only on non-anchor patches. | (pending) | **WCRW (9 BC):** CV mean MSE 0.00039, val MSE **0.000112**. k=0 median **0.98** / P95 2.27 (vs full-pool 3.30 / 7.42); ≤1.5: **100 %**, ≤3: **100 %**. H10b k=13: median **0.88** / P95 2.56; ≤1.5: 100 %. **USFA (7 MOAB):** CV 0.00101, val 0.00137. k=0 median 2.26 / P95 4.68; ≤1.5: 30 %, ≤3: 60 %. H10b: 2.11 / 8.38; ≤1.5: 40 %. **CanvasMatte (5 BC):** val 0.000331. k=0 median 2.58 / P95 4.86; ≤1.5: 0 %. **H10b: 1.73 / 4.10; ≤1.5: 25 %** (−33 % median). | **Two compounding wins.** (1) Per-mode focus is the dominant lever: training one CAE per Epson preset lets the 8-dim substrate latent fit a narrower, homogeneous manifold, dropping validation median 3.4× on WCRW (3.30 → 0.98). (2) H10b anchor fine-tune helps incrementally — small (−10 %) when the per-mode baseline is already tight (WCRW), large (−33 %) when the mode still has substrate sub-clusters that 13 anchors can disambiguate (CanvasMatte's high-OBA vs low-OBA split). H10b P95 sometimes worsens on tiny pools (USFA P95 4.68 → 8.38) — symptom of fine-tune over-adjustment with few anchors against a small held-out set; gradient steps / lr tuning candidates. **H10 verdict updated: H10b CONFIRMED for per-mode CAE.** Original H10 (paper-only, k=0, full pool) remains rejected — that was an unfair comparison; the per-mode + anchor-fine-tune path beats classical predictors on the tight modes (WCRW 0.88 vs C7+S3-neutral ~1.06). | (a) Per-mode CAE_D7 deployment to the frontend: load mode-specific weight bundle (`cae_d7_<preset>.json`) when ref/target share an Epson preset; fall back to the full-pool weights otherwise. (b) Re-train H10b with lower learning rate / more steps to reduce P95 over-adjustment. (c) Extend per-mode coverage to the remaining presets that have ≥ 4 profiles (`Prem Luster`, `EMP`). |
| 2026-05-30 | H12 — single-α OBA emission scaling from anchors (REJECTED)                   | DecorMatte (OBA-extreme, score 1.20) as ref vs three low-OBA Canvas Matte targets (Lyve, BelgianLinen, ChromataWhite). `scripts/experiments/h12_oba_scale.ts` + `lib/predict/obaModelFit.ts` fits per-profile α via closed-form weighted LSQ: for each anchor, take observed OBA contribution = R_anchor − per-anchor poly2-baseline-extrapolation, model = f_default · E_paper · α; pick α minimising Σ residual² over the OBA band. 4 anchor recipes tried (with-yellow, no-yellow, gray-blue, red-only). | (pending) | **α_ref (DecorMatte) varies 0.30 → 1.83 across recipes** (CV ≈ 79 %, well above the 30 % falsification threshold). α_tgt = 1.0 on all low-OBA targets (correct fallback). On all three pairs H12-D7 worsened median ΔE00 by **+0.01 to +0.14** and P95 by +0.00 to +0.16. UV-band RMS unchanged within ±0.5 %. | **H12 REJECTED.** A single per-profile scalar α cannot capture the per-ink visible-band attenuation of OBA emission. Yellow ink has T(440) ≈ 0 — its observed emission contribution is tiny regardless of substrate emission magnitude, so the LSQ fit reads "low α" and the suppression then cancels real emission contribution everywhere else. The right model is per-ink (or at least per-coverage) visible-band attenuation curve, not a profile-level scalar. The user's physical intuition (OBA computed from paper + yellow + colored anchors) is correct, but the math has to scale per-band by the ink's blue transmittance, not by a global emission factor. | (a) Defer the richer per-channel OBA emission model — the data-driven CAE_D7 already absorbs this implicitly. (b) Move to H10b (CAE anchor fine-tune) as the next OBA-handling lever. (c) When/if revisiting: fit per-band attenuation g(λ) from yellow/cyan/red anchors instead of scalar α. |
| 2026-05-30 | H13c (adaptive gate) + plain-D1 baseline reveal D7 wrapper redundant         | AllureAq excluded from the same-mode H4 bucket (its 1550-patch chart doesn't share SAMPLE_IDs cleanly with the 905-patch chart used by every other BC profile → 41+ ΔE alignment artefact, not a predictor failure). Added a **plain D1** column (D1+S1, rank=5, uvBandCount=4, **no D7 wrapper at all**) plus **H13c — adaptive gate**: pick plain D1 when `obaMismatch(papA, papB) < 0.10` (no OBA compensation), else pick H13b. | (pending) | On 90 same-mode BC pairs (AllureAq removed): med-of-medians **plain D1 = 0.800**, **baseline D7 = 0.803**, H13 = 0.986, H13b = 0.856, **H13c = 0.817** (used H13b on 52/90 pairs, plain D1 on 38). P95-of-medians: plain 1.416 / baseline 1.416 / H13b 1.547 / H13c 1.466. Losses ≥ 0.1 ΔE vs baseline: H13 = 42, H13b = 23, H13c = **10**. H4 pass rate (median ≤ 1.5 ∧ P95 ≤ 3.0): plain/baseline/H13c **85.6 %**, H13/H13b 86.7 %. Top H13c wins (where the gate fired H13b): DecorMatte → Lyve −0.09, BagasseText → 600MT −0.09, 800M → BelgianLinen −0.08, DecorMatte → ChromataWhite −0.08. | **Surprising finding: the D7 wrapper is essentially redundant** once D1 carries `residualRank = 5` + `uvBandCount = 4` (introduced 2026-05-29). Plain D1 with no OBA compensation matches baseline D7-default exactly on med-of-medians and P95-of-medians (0.800 / 1.416 vs 0.803 / 1.416). The UV-clamp + high-rank residual already absorb most of the OBA non-linearity — analytic emission subtraction adds variance equal to what it removes on average. **H13c is the right tool but it's a small win on top of an already-tight baseline**: the cheap measured-emission injection helps OBA-disparate pairs by 0.08–0.09 ΔE, but the bulk of pairs benefit more from leaving the spectra alone. The expected `O(0.3 ΔE)` improvement claimed by H13's acceptance criterion was unrealistic given how well rank-5 + UV-clamp already does. | (a) Set H13c as the **default OBA path** in the frontend `TransferView` (replaces the D7-default toggle); plain D1 below `obaMismatch < 0.10`, anchor-driven measured emission above. (b) Strip the D7 analytic-emission code path from the default predictor pipeline — keep `obaSeparator` as an exposable diagnostic only. (c) Article framing: "Once D1's residual is rich enough, OBA fluorescence becomes a small residual signal, not a dominant non-linearity. Measured M0/M2 anchors recover the last 0.08–0.09 ΔE on OBA-disparate pairs but don't materially move the dataset-level median." |
| 2026-05-30 | H13b — anchor-driven kNN emission interpolation (refined H13)                | Same `scripts/experiments/h13_m0m2.ts` runner, third variant: per-anchor measured emission `E_anchor(λ) = M0_anchor − M2_anchor` is sampled at every anchor patch; non-anchor target patches get `E(λ) = Σ_i w_i · E_anchor_i(λ)` with `w_i = 1/d_i^2` and `d_i` the **RGB Euclidean distance** (normalised to [0,1]) to the i-th anchor (k = 4 nearest). Predicted M0 = M2-predicted + interpolated emission. Anchors return their measured M0 directly. | (pending) | On 92 same-mode BC pairs: med-of-medians 0.806 (baseline) / 0.987 (H13 simple) / **0.865 (H13b)**. P95-of-medians 1.44 / 1.62 / **1.57**. Losses ≥ 0.1 ΔE: 43 (H13) → **24 (H13b)** — anchor-driven interpolation cuts the regression by ~half. **Per-mode**: Canvas Matte −0.019, Canvas Satin −0.022 (H13b finally beats baseline on the OBA-disparate modes); WCRW +0.027 (penalty drops 6× vs H13); Premium Glossy +0.015. Wins on OBA-disparate pairs: DecorMatte → Lyve −0.09, DecorMatte → ChromataWhite −0.08, 800M → BelgianLinen −0.08. H4 pass rate 83.7 % (baseline) / 84.8 % (both H13/H13b). | H13b is **the right direction** — using measured emission samples at anchor patches grounds the OBA contribution in real data instead of a global UV factor — but the gain is marginal at the dataset level because (a) the baseline D7 already captures most of the OBA signal via analytic extrapolation, and (b) WCRW (n = 56, 61 % of pairs) has near-zero OBA mismatch where any emission re-projection adds variance. **H13b should be used adaptively** on pairs with `obaMismatch ≥ 0.10`. The AllureAq ↔ ArtPeelBlckt EMP pair stays catastrophic on all three (41+ ΔE) because AllureAq's 1550-patch chart shares no meaningful SAMPLE_IDs with the 905-patch chart — alignment is the bug, not the predictor. | (a) **H13c — adaptive gate**: pick H13b when `obaMismatch(paperA, paperB) ≥ 0.10`, fall back to baseline D7-default otherwise. Predicted to leave WCRW untouched while keeping the 0.08–0.09 ΔE wins on the Canvas Matte OBA-disparate pairs. (b) Filter the EMP-pair from the H4 same-mode bucket entirely until AllureAq's chart mismatch is resolved at the parser/alignment layer. (c) Article framing: "M0/M2 paired data adds about −0.02 ΔE on the OBA-disparate slice; for low-OBA pairs the analytic D7 emission is sufficient." |
| 2026-05-30 | H13 (simple) — measured M0/M2 OBA correction REJECTED on average | Extended `cxfParser` to collect paired **M2** spectra alongside the primary M0 measurement per patch (`spectra_m2` on Measurement). Probed: M2 paired with M0 in **23 of 26 BC profiles**. New script `scripts/experiments/h13_m0m2.ts`: for every same-preset pair, run baseline `D1+S1+D7-default` vs **H13** = D1 trained on M2 spectra (no UV clamp), then re-add emission `E_patch(λ) = E_paper(λ) · u(patch)` where `E_paper = R_paper_m0 − R_paper_m2` is measured and `u(patch) = R_patch_m2(380) / R_paper_m2(380)` is a simple UV-attenuation factor. Evaluated on 92 same-mode BC pairs against the true M0 spectra. | (pending) | **REJECTED** on the average pair: H13 raises median-of-medians **0.806 → 0.987** (+0.18) and median P95 1.44 → 1.62 (+0.18); zero pairs win by ≥ 0.1 ΔE while 43/92 lose by ≥ 0.1 ΔE. Per-mode: WCRW (low-OBA, n=56) hurt worst (+0.17); Canvas Matte (n=20) +0.08 on average but **wins on the OBA-extreme pairs**: DecorMatte → Lyve −0.09, 800M → BelgianLinen −0.09, DecorMatte → ChromataWhite −0.06. H4 pass rate edges up 83.7 % → 84.8 % (more pairs squeak under the bound) but the median quality drops. | The simple paper-scaled emission model is too crude: when paper OBA is near zero (most WCRW pairs) the tiny measured `E_paper` carries measurement noise and `×u(patch)` amplifies it onto every patch. Where H13 does help, it's on the OBA-disparate pairs — exactly the physically motivated case — but the average hurt is larger than the targeted gain. The clamp-free `D1 on M2` step alone produces tighter substrate transforms; the failure mode is the emission **re-projection** onto non-anchor target patches. | **H13b — anchor-driven emission interpolation.** Use the **measured `E_anchor(λ) = M0_anchor − M2_anchor`** at each of the k anchor patches as direct emission samples, then estimate non-anchor `E_patch(λ)` via RGB-distance-weighted interpolation across anchors (or fit per-band scale = ϕ(rgb) from anchor residuals). This grounds emission in measurements at multiple ink-coverage points instead of paper alone. Predicted to win on the OBA-disparate pairs without hurting low-OBA pairs (anchor emissions ≈ 0 there → no scaling noise added). |
| 2026-05-30 | H4 batch extended — CAE_D7 k=0 column alongside D1+S1+D7 (D1 vs CAE head-to-head) | `scripts/experiments/h4_batch.ts` now also runs `runCAETransfer` (CAE_D7) at **k = 0** (paper-only) per pair, with `pickCaeBundle(refPreset, tgtPreset)` choosing the per-Epson-preset weight bundle when both profiles share a mode (CanvasMatte / WCRW / USFA / PremiumLuster), otherwise falling back to the 36-profile mixed-pool model. Per-pair JSON gains `cae_median` / `cae_p95` / `cae_mode` / `cae_modeMatched` columns. | (pending) | D1+S1+D7 vs CAE_D7 k=0 across the **same 600 BC pairs**: **same-mode (n=98)** D1 med-of-meds 0.84 / pass 80.6 % vs CAE_D7 1.17 / pass 53.1 %. **Cross-mode (n=502)** D1 2.47 / 0.8 % vs CAE_D7 6.06 / 0.0 %. **All (n=600)** D1 2.36 / 13.8 % vs CAE_D7 5.85 / 8.7 %. | Two clean stories for the article: **(1) Same-mode** — D1 with 13 anchors beats the per-mode CAE at k = 0 (0.84 vs 1.17 ΔE00) because the anchors deliver the substrate signal the CAE has to infer; the CAE alone still reaches 53 % H4 pass paper-only, a useful "no measurement" baseline. **(2) Cross-mode** — neither path works at this anchor count; CAE's full36 fallback is much worse (6.06 vs 2.47) because the mixed-pool manifold is too wide. Cross-mode transfer is the H10b territory: per-mode CAE + anchor fine-tune with L2 reg (already proven on USFA: P95 −15 % at l2 = 0.5). | (a) Add a **CAE+anchor finetune** column to `h4_batch.ts` (port `evaluate.py`'s `finetune_sub_b` with `l2_init` to TS) so a final 4-way comparison (D1+S1, D1+S2, CAE_D7 k=0, CAE_D7 k=13+L2) goes into the article plot. (b) Filter the per-mode CAE picker to only fire when the bundle exists *and* validation is honest (skip PremiumLuster's degenerate 3/1/1 case if so chosen). (c) Re-write the article narrative around the same-mode / cross-mode dichotomy. |
| 2026-05-30 | H4 batch — D1+S1+D7 over 600 BC P9000 pairs, same-mode vs cross-mode split   | `scripts/experiments/h4_batch.ts` walks every ordered same-chart pair of the 27 BC P9000 profiles (702 directed pairs; 600 with ≥100 shared SAMPLE_IDs after dropping `AllureAq`'s 1550-patch chart and its non-overlap targets). For each pair, **D1** runs with current TransferView defaults: `residualRank=5`, `uvBandCount=4` (per-band UV clamp `[0.1, 7.0]` on 380-410 nm), **D7 OBA-separation** ON, S1 forced anchors (k=13). Held-out evaluation = the 905 − 13 ≈ 892 non-anchor patches per pair. Per-pair table at `frontend/data/cae-input/h4_batch.json`. | (pending) | **Dataset-wide (n=600)**: median-of-medians ΔE00 = **2.358**, median-of-P95s = 6.00, fraction median ≤ 1.5 = **17.0 %**, fraction P95 ≤ 3.0 = **13.8 %**, **H4 pass (both) = 13.8 %**. **Same-mode subset (n=98)**: median-of-medians = **0.84**, **H4 pass = 80.6 %**. **Cross-mode subset (n=502)**: median-of-medians = 2.47, H4 pass = 0.8 %. Top-5 worst pairs (all cross-mode): DecorMatte CanvasMatte → PhotoPeelGloss PGPP (median 3.94, P95 10.96), PhotoPeelMatte SWM → PhotoPeelGloss PGPP (3.90 / 10.44), DecorMatte → VibranceGloss PGPP (3.86), DecorMatte → 17MSatin CanvasSatin (3.74), DecorMatte → RiverStone PLPP260 (3.69). | **H4 REJECTED dataset-wide but HOLDS when scoped to a single Epson media preset.** The 80 % acceptance threshold is met (80.6 %) on the 98 same-mode BC pairs at k = 13 — within-preset, the D1 paper-ratio + low-rank residual with D7 OBA cleaning is genuinely a few-shot cross-substrate transfer. Cross-preset (different ink mode, total ink limit, driver media setting) lies outside the S1 forced-anchor + D1 assumption space — virtually 0 % pass. The DecorMatte outlier cluster confirms the OBA-extreme + cross-mode pairing is the worst-case. | (a) Re-scope H4 in `RESEARCH_HYPOTHESIS.md` to **same Epson preset pairs** (new acceptance: ≥ 80 % of same-mode pairs ≤ 1.5 ΔE00, met). (b) For cross-mode transfer, drop the k = 13 / D1 baseline and quantify the gap a CAE_D7 per-mode model leaves at k = 0 vs k = 13 with anchor fine-tune. (c) Add a "same-mode only" toggle to the H4 batch runner and the article narrative. |
| 2026-05-30 | CAE_D7 retrained on 36-MK pool with 3-set split + 5-fold CV                  | New `split.py`: deterministic 3-way 60/20/20 = **train 22 / test 7 / validation 7** (seed 42, sorted alphabetically within each set). New `cv_train.py`: 5-fold cross-validation over the `train ∪ test` pool (29 profiles, 23-or-24 train / 5-or-6 eval per fold, 50 epochs/fold, Adam lr 1e-3, batch 256, ID-dropout 30 %). Final model trained on the full 29-profile pool while monitoring the **validation** set (never seen during CV). `evaluate.py` gains `--set {test, validation}` (default validation). | (pending) | **CV (5-fold)**: per-fold best MSE [0.00279, 0.00185, 0.00207, 0.00196, 0.00163] → mean **0.00206 ± 0.00039**. **Final model**: validation MSE **0.001495**. Validation per-pair ΔE00 (paper-only, k = 0 anchors): **median-of-medians 3.30**, median-of-P95s 7.42, fraction ≤ 1.5 = 1.0 %, fraction ≤ 3.0 = 34 %. Per validation target (median across 29 ref profiles): MOAB Entrada Rag Natural USFA **1.77**, BC_ChromataWhite CanvasMatte 2.59, BC_800M CanvasMatte 3.16, BC_Signa270 WCRW 3.37, BC_PuraVelvet WCRW 3.49, BC_BagasseSmooth WCRW 4.22, BC_VibranceMetallic PremiumGlossy **4.50**. | The retrained CAE_D7 is **competitive on substrate categories well-represented in the training pool** (MOAB USFA target = 1.77 median, near the H11 cross-vendor floor) and **degrades on under-represented or physically off-preset substrates** (BC matte/canvas, the metallic VibranceMetallic). With the broader 36-profile pool the model can no longer rely on the narrow CanvasMatte / WCRW substrate manifold the previous 16-profile run sat on, so the median-of-medians regresses from 1.66 to 3.30 — honest k = 0 generalisation across 10 Epson presets is harder than k = 0 within 2-3 presets. CV variance is small (σ/μ ≈ 19 %) → the model is stable, just under-capacity for the broader manifold. | (a) **Anchor fine-tune at inference (H10b)**: feed the same k anchors the classical predictors use to update `substrate_latent_B` before decoding. Expected 0.5–1 ΔE drop. (b) **Per-print-mode CAE models** — one per Epson preset, trained on its own subset. Trades generalisation breadth for accuracy within mode. (c) Larger model (deeper substrate encoder, wider hidden dim) to fit the broader manifold, watching CV variance for overfit. |
| 2026-05-30 | H11 with WLS interpolant — cross-vendor device response, per-pair breakdown   | Same 44 profiles. `modeCompare.ts` defaults switched to `MODE_INTERP=wls` (local-linear weighted-least-squares interpolation per band: at each grid point fit `R(λ) ≈ β₀ + β·rgb` from k=20 weighted neighbours via Cholesky on the 4×4 normal equations, IDW fallback for collinear neighbours), `MODE_KNN=20`, `MODE_GRID_LEVELS=11`. `MODE_NORM=device` retained (OBA-clean + paper-relative to common reference paper). New per-pair table in `docs/mode-comparison.md`. | (pending) | Cross-set median ΔE00 vs IDW: **Canvas Matte 1.62 → 1.20** (P95 3.48 → **3.28**), **Premium Luster 1.75 → 1.38** (P95 3.79 → **3.27**), **Premium Glossy 1.88 → 1.34** (P95 4.39 → **3.96**). Interpolation noise floor halved: BC ΔE00 median 1.81 → **0.66**, MOAB 1.33 → **0.43**. Per-pair worst-P95 outliers: CanvasMatte = DecorMatte×Anasazi (4.00), PremiumLuster = VibranceLuster×ColoradoFiberSatine (4.06), PremiumGlossy = PhotoPeelGloss×LasalGloss (4.70). Most BC×MOAB pairs now P95 < 3. | **H11 holds with sharper signal:** with WLS, the cross-set median is **1.2–1.4 ΔE00** on the three overlapping presets — well into perceptually-equivalent range — and clearly above the BC interpolation floor (now ~0.7 ΔE00), so what we measure is real device-response residual rather than interpolation noise. **P95 outliers are physically grounded, not interpolation artefacts**: the worst BC×MOAB pairs all involve OBA-extreme BC papers (DecorMatte, 800M, PhotoPeelGloss) where the D7 OBA-emission extraction is approximate — vendor OBA chemistry differs and the quadratic extrapolation under-models it. Notably **the metallic `VibranceMetallic` is NOT a Premium Glossy outlier** (P95 1.91, the best of the three pairs), contrary to the hypothesis we floated before the breakdown. | (a) Refine D7 OBA-emission model on multi-vendor papers (per-band fit beyond quadratic) and re-test the OBA-extreme pairs. (b) Drop `PhotoPeelGloss` (peel-and-stick substrate, physically distinct from photo paper) from the Premium Glossy H11 set or relabel. (c) Run the CAE retrain (Stage 3) on the new dataset (44 profiles, mode-grouped) and target median ≤ 1.0 with the WLS-resampled training set. |
| 2026-05-29 | D1 defaults tuned for OBA-disparate pairs (rank 5, UV-aware clamp, D7 on)     | DecorMatte (the OBA-extreme paper in Canvas Matte: R(380)=0.117, OBA 1.196, Z=-1.56 within cluster, dataset-wide rank #2 by UV absorption) paired with three same-mode targets at S1 (k=13). `scripts/experiments/analyzeDecorMatte.ts` quantified its uniqueness; `scripts/experiments/compareD1Defaults.ts` measured the five-way ablation. Three changes: (1) D1 default `residualRank` 2 → 5; (2) per-band clamp via new `ratioClampUV=[0.1, 7.0]` + `uvBandCount=4` on the 380–410 nm bands (uniform `[0.3, 3.0]` kept for 420–730 nm); (3) D7 OBA-separate ON by default. | (pending) | **DecorMatte ↔ Lyve:** OLD 1.447 / 5.099 → NEW 1.416 / **4.057** (P95 −20%). **↔ BelgianLinen:** 1.659 / 4.726 → 1.468 / **4.007** (median −12%, P95 −15%). **↔ ChromataWhite:** 1.928 / 7.471 → 1.886 / **6.419** (P95 −14%). Clamped bands 2 → **0** on every pair (UV info preserved). Paper-ratio at 380 nm for these pairs: 5.05–5.94×, well above the legacy [0.3, 3.0] clamp. | The three changes are complementary — UV-clamp alone *hurts* (1.659 → 1.727 P95 4.7 → 5.9 on BelgianLinen, the wider band admits noise without the D7 stabiliser), but with rank=5 + D7 the wider clamp keeps the signal cleanly. Median is mostly moved by rank=5 (confirms the H5 finding: rank-2 systematically under-captures the OBA + ink-coverage structure on top of paper white). P95 — the worst-patch behaviour — drops 14–20% across the board with zero band truncation. New defaults shipped in `TransferView`. | (a) Re-run mode-comparison (cross-set H11) on the new defaults — expect Premium Glossy 3.36 → ~2 with the OBA-disparate pairs better handled. (b) Add an OBA-band anchor strategy (very light grays where paper UV is most exposed) on top. (c) Re-train CAE_D7 with these defaults' anchored variants. |
| 2026-05-29 | H5 rank-distribution — effective rank of the substrate-transform difference   | 44 profiles → 461 same-chart pairs (BC-BC + MOAB-MOAB, matched by rounded RGB, ≥100 patches). `scripts/experiments/h5_rank_distribution.ts`: D = X_B − X_A (N×36), eigenvalues of DᵀD, smallest rank capturing ≥99% Frobenius energy. | (pending) | **Median effective rank 5, max 9.** r≤4: **17.8%** (acceptance needs ≥90%); r≤6: 83.3% (16.7% need r>6, falsification ≤10%). Histogram peaks at r=5. | **H5 REJECTED at 99% energy.** The raw substrate-transform difference needs ~5–6 components, not ≤4 — paper-white shift (1) + OBA band structure (1–2) + ink-coverage interaction (2–3). D1's rank-≤3 residual therefore under-captures; a rank-~5 residual or A3's per-λ affine is better matched to the data. | (a) Re-run on the *device-normalised* difference (substrate removed) — expect lower rank if the residual is mostly substrate. (b) Raise D1's residual rank from ≤3 to ~5 and re-test transfer accuracy. (c) At a looser 95% threshold the rank is lower; the 99% bar is what fails. |
| 2026-05-29 | H11 with substrate normalisation — OBA-clean + paper-relative device response | Same 44 profiles. `modeCompare.ts MODE_NORM=device`: per profile remove OBA fluorescence (D7 quadratic-extrapolation emission, per-patch factor R(380)/paper(380)), take paper-relative ratio of the cleaned spectrum, re-apply a common reference paper (mean cleaned paper across all profiles, ratio cap 4), then compare on the 9³ RGB lattice. Compared to raw (`none`) and OBA-only (`oba`). | (pending) | Cross-set median ΔE00, none → oba → **device**: Canvas Matte 2.16 → 1.95 → **1.62**; Premium Luster 2.28 → 2.28 → **1.75**; Premium Glossy 3.36 → 3.36 → **1.88**. BC interpolation floor 1.83, MOAB 1.33 — the device-normalised cross-set residual is at/below the BC floor. | **H11 CONFIRMED for all three overlapping presets.** After factoring out paper white + OBA, two vendors' profiles for the same Epson preset agree to ΔE00 ≤ 1.9 — at the interpolation noise floor, i.e. device responses are effectively identical and the raw 2–3 ΔE gap was *substrate*, exactly as H11 predicts. Premium Glossy (raw 3.36, marginal) passes cleanly at 1.88. OBA-only helps the OBA-disparate Canvas Matte pairs but not Luster/Glossy; the paper-relative step is what closes the gap. PCA-score interpolation was rejected (it raised the floor 1.81→1.98 on BC) — the interpolation bottleneck is spatial sampling, not spectral noise. | (a) Better spatial interpolant (local-linear WLS) to lower the ~1.8 BC floor and sharpen the device estimate. (b) Run the H5 SVD rank test to confirm the substrate transform is low-rank. (c) AllureAq (EnhancedMatte) and Moenkopi Bizan (VelvetFineArt) are genuinely different substrates — within-mode outliers to flag, not fix. |
| 2026-05-29 | Print-mode taxonomy + cross-vendor device-response comparison (H11)           | 44 P9000 profiles loaded (27 BC + 17 MOAB with spectra; `MOAB Slickrock Silver` lacks CGATS spectra) reorganised into 10 Epson-preset folders. Cross-set BC↔MOAB compared on a common 9×9×9 RGB lattice via per-band k-NN IDW (k=8, power=2). See `docs/mode-comparison.md` for full per-preset tables. | (pending) | Overlapping presets, cross-set median ΔE00: **Canvas Matte 2.16** (P95 3.87; within-BC 1.92), **Premium Luster 2.28** (P95 4.41; within-MOAB **0.98**, within-BC 3.27), **Premium Glossy 3.36** (P95 5.50; within-BC 2.24). Interpolation noise floor ≈ 0.020–0.026 reflectance RMS. MOAB Prem Luster intra-group extremely tight (spectral r=0.9978). | **H11 holds for Canvas Matte and Premium Luster** — cross-set median ΔE00 ≤ 3 and comparable to within-set spread, so two vendors' profiles for the same Epson preset share device response within ~2 ΔE00. **Premium Glossy is marginal (3.36 > 3)** but only one MOAB glossy survived (Slickrock Silver had no spectra) and BC glossy includes the metallic `VibranceMetallic` outlier (paper L\*87). MOAB same-mode charts are far tighter internally than cross-vendor: same Epson preset ≠ identical device response across vendors, but the gap is small. OBA score spans 0.75–1.35 across presets, confirming substrate/OBA dominates the residual. | (a) Recover a second MOAB glossy with spectra (or drop `VibranceMetallic`) and re-test Premium Glossy. (b) Add paper-relative normalisation before the grid ΔE00 to separate device response from substrate white. (c) If the 0.02 noise floor limits resolution, swap k-NN IDW for per-band RBF on a thinned sample. |
| 2026-05-27 | MOAB P9000 ICC spectral extraction and same-print-mode correlation audit     | `data/profiles/2023 Epson SureColor P9000 MOAB Profiles`: 18 ICC files grouped by filename print mode (`USFA`, `Prem Luster`, `Prem Semigloss`, `VFA`, etc.). Spectra extracted from ICC `targ` CGATS.17 text tags when present. Pairwise correlations computed on common rounded RGB patch coordinates inside each print mode.                                                          | (pending) | Spectral extraction coverage: **17/18 ICC** files; missing spectral CGATS in `MOAB Slickrock Silver P9000 Prem Glossy.icc`. Usable groups: Prem Luster 4 profiles / mean r=0.9978 / mean R²=0.9957 / RMSE=0.0186; Prem Semigloss 2 / r=0.9979 / R²=0.9957 / RMSE=0.0291; USFA 7 / mean r=0.9977 / mean R²=0.9954 / RMSE=0.0279; VFA 2 / r=0.9866 / R²=0.9733 / RMSE=0.0530.                                                                                                     | The MOAB profiles are usable for same-print-mode CAE training, but the previous CAE data path was not explicitly enforcing print-mode homogeneity. Some profiles share a print mode but not an identical target grid (`Entrada Rag Textured` has 1877 patches, `Moenkopi Bizan` 2058, several targets use fractional RGB steps), so training/export must keep `print_mode` and avoid assuming row-index equivalence.                                                                                                                                                                                                              | Train/evaluate CAE per print mode (`CAE_PRINT_MODE=USFA`, `Prem Luster`, etc.) and compare against the mixed-mode baseline. For anchor experiments, align candidate patches by RGB/device coordinates or by a known target definition, not by row number alone.                                                                                                                                                                     |
| 2026-05-25 | D7-CAE-M0 vs D7-CAE-M1 — Comparing measurement conditions (M0 vs M1)         | 10 train / 5 held-out split (seed 42, excluding BC_AllureAq_P9000_MK_EMP with 1550 patches). Arch: paper+ID(10) → 32 → 8 substrate latent; R+RGB+sub_lat → 64 → 16 ink latent; mirror decoder. 30 epochs Adam lr 1e-3, batch 256. ID dropout 30%. Loss = MSE + 0.1·‖ink_lat_A − ink_lat_B‖².                                                                                             | (pending) | D7-CAE-M0: **median-of-medians ΔE00 = 1.66**, median-of-P95s = 4.58, **40% of pairs achieve median ≤ 1.5**. D7-CAE-M1: median-of-medians ΔE00 = 3.12, median-of-P95s = 6.59, **0% of pairs achieve median ≤ 1.5**.                                                                                                                                                                                                                                                              | **D7-CAE-M0 substantially outperforms D7-CAE-M1**, indicating that standard M0 measurements provide better spectral data for substrate transfer modeling than M1 measurements (which include UV). The OBA preprocessing in D7-CAE is particularly beneficial with M0 data where UV effects can be properly modeled and removed, while M1 data introduces noise that degrades CAE performance despite OBA cleaning.                                                                                                                                                                                                                | (a) Consider whether M2 (UV-cut) measurements might provide intermediate performance. (b) Investigate if the performance difference is primarily in the UV range (380-400nm) where M0 and M1 differ most. (c) Use M0 as the standard measurement condition for future CAE training.                                                                                                                                                 |
| 2026-05-25 | D7-CAE — Conditional Autoencoder on OBA-cleaned spectra (M0 measurements)    | 10 train / 5 held-out split (seed 42, excluding BC_AllureAq_P9000_MK_EMP with 1550 patches). Arch: paper+ID(10) → 32 → 8 substrate latent; R+RGB+sub_lat → 64 → 16 ink latent; mirror decoder. 30 epochs Adam lr 1e-3, batch 256. ID dropout 30%. Loss = MSE + 0.1·‖ink_lat_A − ink_lat_B‖².                                                                                             | (pending) | Python `evaluate.py` summary: **median-of-medians ΔE00 = 1.66**, median-of-P95s = 4.58, **40% of pairs achieve median ≤ 1.5**. App head-to-head on DecorMatte→Lyve (TRAIN pair): A3 1.54, D1 1.45, B3 2.13, C7 1.28, **CAE_D7 1.78**. On 600MT→OpticaOne (held-out pair): A3 0.64, D1 0.60, B3 0.68, C7 0.52, **CAE_D7 1.60**.                                                                                                                                                  | D7-CAE shows substantial improvement over CAE_RAW (median-of-medians ΔE00: 3.20 → 1.66) by training on OBA-cleaned spectra. The OBA removal preprocessing helps the CAE learn cleaner substrate-transfer functions. Still underperforms analytical predictors like C7 on anchored tasks due to lack of target-specific anchor information, but provides a strong k=0 baseline for substrate transfer.                                                                                                                                                                                                                             | (a) Export weights to frontend for CAE_D7 predictor integration. (b) Consider hybrid approach: use D7-CAE as substrate encoder with anchor fine-tuning. (c) Investigate remaining error sources — likely OBA estimation inaccuracies and residual substrate-ink interactions.                                                                                                                                                       |
| 2026-05-24 | Phase 8 CAE_RAW — Conditional Autoencoder cross-trained on 11/16 MK profiles | 11 train / 5 held-out split (seed 42). Arch: paper+ID(12) → 32 → 8 substrate latent; R+RGB+sub_lat → 64 → 16 ink latent; mirror decoder. 30 epochs Adam lr 1e-3, batch 256. ID dropout 30%. Loss = MSE + 0.1·‖ink_lat_A − ink_lat_B‖². 99,550 train pairs, 18,100 held-out pairs.                                                                                                        | (pending) | Best held-out MSE: 0.0009 (raw reflectance, train-test). Python `evaluate.py` summary: **median-of-medians ΔE00 = 3.20**, median-of-P95s = 8.29, **0% of pairs achieve median ≤ 1.5**. App head-to-head on DecorMatte→Lyve (TRAIN pair): A3 1.54, D1 1.45, B3 2.13, C7 1.28, **CAE_RAW 1.94**. On 600MT→OpticaOne (held-out pair): A3 0.64, D1 0.60, B3 0.68, C7 0.52, **CAE_RAW 2.35**. AllureAq outlier explodes (29 ΔE) — substrate class very different from training pool. | **H10 REJECTED in current form.** CAE without anchor fine-tuning consistently underperforms because A3/D1/B3/C7 see 13 anchors of MEASURED target reflectance, while CAE sees only the paper white spectrum. The model has done what it was trained for (cross-substrate generalization, MSE 0.0009 on reflectance), but the task is fundamentally asymmetric: knowing 13 (RGB, R) pairs on target is more information than knowing only paper. CAE shines exactly where other predictors cannot work — k=0 anchors, paper-only.                                                                                                  | (a) Add few-shot anchor fine-tune: 1-step gradient on substrate_latent_B from k anchors at inference (~50 ms). Expected: 0.5–1 ΔE drop. (b) Investigate AllureAq outlier — likely needs to be moved to held-out (currently in train). (c) Train D7-CAE variant (Stage 2) for OBA-disparate pair test. (d) Consider conditioning on (paper_A, paper_B, R_A, RGB, k anchors) explicitly — turns CAE into "few-shot transfer learner". |
| 2026-05-24 | D7 OBA-separation wrapper — predictor-agnostic, no extra anchor              | DecorMatte (ref, heavy OBA, score 1.20) vs Lyve (target, no OBA, score 1.02). D7 extracts emission analytically from each paper spectrum: deg-2 poly fit on R_paper(λ) ∈ [460, 730] → extrapolate back to [380, 450] → emission = max(0, R_paper − base). Per-patch OBA factor = clamp(R_patch(380)/R_paper(380), 0, 1). Subtract → run predictor on clean → add target's emission back. | (pending) | Extracted OBA peak: A=0.106 @ 410 nm (DecorMatte), B=0.000 (Lyve no OBA, as expected). Head-to-head D7 OFF vs ON on this pair: A3 1.54→1.50, D1 1.45→1.42, B3 2.17→2.21 (slight worse), **C7 1.28→1.30** (slight worse). On S3 cyan (k=5): A3 9.31→9.23, **D1 2.93→2.42 (−0.51)**, C7 9.28→8.88 (−0.40).                                                                                                                                                                        | D7 helps directionally as designed. Biggest win: D1 on S3 cyan, where the catastrophic OBA failure mode (D1=2.93 raw, A3=9.31 raw) is materially mitigated by removing OBA structurally. On S1 (k=13) the wins are modest because **this pair has OBA in the REF, not the target** — D7 cleans ref → matches target naturally → small delta. Reverse direction (Lyve→DecorMatte, target=heavy OBA) will exercise the "add OBA back" path more visibly. B3 slightly degrades because pool basis was rebuilt from cleaned spectra using target's emission as proxy for all pool — imperfect but bounded.                            | (a) Run Lyve→DecorMatte to confirm "add-OBA-back" helps when target has the OBA. (b) Batch test on multiple OBA-disparate pairs (Phase 8). (c) Improve per-pool-profile OBA extraction in B3 path (currently uses target's emission for all pool, should use each pool profile's own paper).                                                                                                                                        |
| 2026-05-24 | C7 per-λ monotone curve — 4-way head-to-head with S1 anchors                 | DecorMatte (ref) vs Lyve (target), both CanvasMatte. Anchor strategy S1 (13 anchors). All four predictors run.                                                                                                                                                                                                                                                                           | (pending) | A3 median 1.54, D1 1.45, B3 (rank 6) 2.17, **C7 1.28**. C7 wins by 0.17 ΔE00 over D1, by 0.26 over A3.                                                                                                                                                                                                                                                                                                                                                                          | C7's per-λ piecewise-linear monotone fit beats both A3's linear-only fit AND D1's paper-ratio+residual decomposition at the same anchor budget. Validates the hypothesis that substrate transform is a non-linear-but-monotone function of reference reflectance per λ, not a constant slope (A3) and not strictly multiplicative (D1).                                                                                                                                                                                                                                                                                           | Test C7 with S3 ramp anchors to see whether sharing across inks holds; verify on more OBA-disparate pairs (PuraSmooth vs DecorMatte).                                                                                                                                                                                                                                                                                               |
| 2026-05-24 | S3 neutral ramp (k=5) — C7 dominates with fewer anchors than S1 (k=13)       | DecorMatte ↔ Lyve. Anchors: paper + 4 neutral gray patches at R=G=B={192,128,64,0}. k=5 total.                                                                                                                                                                                                                                                                                           | (pending) | A3: 1.53, D1: 1.42, B3: **27.00 (catastrophic)**, **C7: 1.06**. Winner: C7 by 0.36 ΔE00. C7 with k=5 beats every predictor at k=13 on this pair.                                                                                                                                                                                                                                                                                                                                | **User's hypothesis confirmed for neutral ramp:** substrate transform is shared across inks when the anchor ramp visits all reflectance levels at each wavelength. Neutral gray touches C, M, Y proportionally → spans the (A, B) plane at every λ → per-λ curve is well-determined. Anchor budget drops from 13 to 5 (62% reduction) AND median ΔE00 improves from 1.28 to 1.06. B3 collapses because its pool basis was tuned for spread-anchor coverage; 5 collinear neutrals leave the diagonal score map underdetermined.                                                                                                    | Confirm on at least 2 more OBA-disparate pairs (PuraSmooth ↔ DecorMatte). Investigate the failure mode for B3 + S3 neutral.                                                                                                                                                                                                                                                                                                         |
| 2026-05-24 | S3 cyan ramp (k=5) — counterexample: single-channel ramp FAILS at short λ    | DecorMatte ↔ Lyve. Anchors: paper + 4 cyan-ramp patches at G=B=255, R={192,128,64,0}. k=5.                                                                                                                                                                                                                                                                                               | (pending) | A3: 9.31, D1: 2.93, B3: 21.28, C7: 9.28. Winner: D1. C7 explodes.                                                                                                                                                                                                                                                                                                                                                                                                               | **OBA hypothesis confirmed (user reminder).** Cyan ink is transparent in the UV/blue region (380–410 nm), so the cyan ramp does not vary A(λ) at those wavelengths — all 5 anchors collapse to ~the same A value at 380 nm. The per-λ curve at 380 nm has effectively one data point + endpoint extrapolation → predictor cannot model OBA-driven substrate change at short λ. D1 partially survives because its paper-ratio extracts r(380) from the single paper anchor, but the residual stage has no signal there. Bottom line: S3 single-channel ramps work only if the chosen channel absorbs across all λ — cyan does not. | (a) Try S3 + magenta or yellow on this pair (magenta absorbs blue, may rescue OBA bands). (b) Hybrid ramp: paper + neutral + a 1-anchor "blue boost" (e.g. dark cyan or near-blue) to ground UV-blue curve. (c) Add UI warning when S3 ramp channel has poor λ-coverage.                                                                                                                                                            |
| 2026-05-24 | S2 greedy adaptive anchor selection on D1, OBA-disparate pair                | DecorMatte (ref, no OBA) vs Lyve (target, OBA), both CanvasMatte. Predictor D1 (rank 2, clamp). Greedy target 0.8 ΔE00, max k = 40.                                                                                                                                                                                                                                                      | (pending) | Seed k=13: median ΔE00 = 1.45. After 28 iterations (k=40): median **1.33**, P95 3.09, R² 0.941, RMS 0.0167. Did NOT converge to target 0.8 — hit k cap. Trajectory: 1.45 → 1.44 → 1.48 → … → 1.36 → 1.33 (non-monotone; greedy occasionally adds a patch that helps locally but worsens median).                                                                                                                                                                                | **Empirical floor for D1 on this pair ≈ 1.33 ΔE00 even at k=40.** Greedy cannot push below — physics of OBA-ink-coverage interaction has an irreducible residual at saturated patches. P95 dropped 5.36 → 3.09 (43% reduction) which is the real win — outliers tamed even though median plateaus. Anchor doubling (13 → 40) buys headroom on worst patches, not on typical patches.                                                                                                                                                                                                                                              | (a) Try S2 with A3 to see whether per-λ affine has a different floor (likely worse). (b) Investigate which patches were added — likely concentrated in CMY saturation regions and OBA-affected blue patches. (c) For end-user: this means D1 needs ≥ 13 anchors for "median 1.5" deals, but going beyond ~25 anchors has diminishing returns on median — focus on outlier-safety instead.                                           |
| 2026-05-24 | 3-way head-to-head A3 vs D1 vs B3 on OBA-disparate pair, 7-profile pool      | DecorMatte (ref, no OBA) vs Lyve (target, OBA), both CanvasMatte. Pool: 8 loaded profiles (target auto-excluded → 7-profile pool spanning CanvasMatte, WCRW, PGPP classes).                                                                                                                                                                                                              | (pending) | A3: median 1.54, P95 5.94, R²=0.428. D1 (rank 2, clamp): median 1.45, P95 5.36, R²=0.846. **B3 (basis rank 6, diagonal score map): median 2.17, P95 14.56, R²=-0.165**. Winner: D1.                                                                                                                                                                                                                                                                                             | B3 underperforms both A3 and D1 on this pair. Diagnosis: (a) pool too small (7 profiles) to cover OBA + ink-coverage variation; (b) diagonal score map cannot capture cross-PC coupling, so OBA-vs-ink interaction goes uncorrected; (c) high P95 (14.56) shows B3 has a few catastrophic patches — likely saturated ink loads where the pool basis under-represents the ink-stacking direction. Honest finding: pool-PCA with diagonal score mapping is the wrong architecture for OBA-dominated cross-substrate transfer.                                                                                                       | (a) Larger pool (load all 27 profiles) — basis rank stays at 6 but eigenvectors are better-conditioned. (b) Full-M score map (B2 variant from plan) — adds cross-PC coupling at 6× param cost. (c) Pool-PCA + residual hybrid: use pool basis as smoother + per-pair PCA residual à la D1.                                                                                                                                          |
| 2026-05-23 | OBA-aware D1 head-to-head on OBA-disparate same-class pair                   | BC_DecorMatte (no OBA, R380=0.117, score=1.195) vs BC_Lyve (OBA-loaded, R380=0.663, score=1.017), both CanvasMatte                                                                                                                                                                                                                                                                       | (pending) | OBA mismatch = 0.179 (high). A3: median ΔE00=1.54, P95=5.94, R²=0.428. D1 (rank 2, clamp [0.3,3.0]): median=1.45, P95=5.36, R²=0.846. D1 wins by 0.09 ΔE00 and 2× higher R². Clamped bands = 2/36 (380, 390 nm where raw ratio = 5.67×, 4.82×).                                                                                                                                                                                                                                 | **D1 with ratio clamp meets H4 (≤1.5) on this pair; A3 misses by 0.04 even though both substrates are in the same "CanvasMatte" class.** OBA mismatch IS the dominant non-linearity in cross-substrate transfer on the P9000 dataset, and D1's structural assumption (multiplicative paper transform + low-rank residual) captures it better than A3's per-λ affine. The ratio clamp prevents D1 from exploding at 380–390 nm while keeping the rest of the spectrum's structure intact.                                                                                                                                          | (a) Repeat on more OBA-disparate pairs (PuraSmooth 0.819 vs DecorMatte 0.117) to confirm the win generalises. (b) Investigate why P95 ΔE00 is still 5.36 — likely concentrated at the same UV-blue bands and at saturated patches. (c) Add per-λ-weighted ΔE in evaluator (down-weight 380–410 nm) and re-run; predict median drops below 1.0 for D1.                                                                               |
| 2026-05-23 | OBA (Optical Brightener) range across 8 P9000 substrates                     | VibranceGloss/PhotoPeelGloss (PGPP), 600MT/PuraSmooth (WCRW), DecorMatte/Lyve (CanvasMatte), 17MGloss/Crystalline (CanvasSatin)                                                                                                                                                                                                                                                          | 1e73dc6   | Paper-white R(380 nm): min 0.117 (DecorMatte), max 0.819 (PuraSmooth) — **7× spread**. R(440 nm) ranges 0.865 → 1.097; values > 1 confirm OBA fluorescence under D50/M0. **Same substrate-class name does NOT imply same OBA** — Lyve (0.663) vs DecorMatte (0.117), both CanvasMatte.                                                                                                                                                                                          | OBA mismatch dominates the cross-substrate signal in the 380–410 nm region. D1 `r(λ) = B_paper/A_paper` blows up there for OBA-disparate pairs, and the ratio is non-linear in ink coverage (ink absorbs UV, kills fluorescence). A3 per-λ affine is more robust because each λ has independent (a, b), but the constant-slope assumption still fails because the substrate effect depends on patch coverage.                                                                                                                                                                                                                     | (a) Clamp D1 ratio to a sane range (e.g. [0.3, 3.0]). (b) Detect OBA via paper-white spectral shape and warn in UI. (c) Investigate whether the 4 M2 (UV-cut) profiles in the dataset give cleaner substrate transforms — possibly use M2 paper-white as the WP reference. (d) Down-weight 380–410 nm in ΔE00 evaluation since CIE Y ≈ 0 there and the perceptual contribution is small.                                            |
| 2026-05-23 | A3 vs D1 head-to-head on near-multiplicative substrate pair                  | BC_17MGloss_pk_CanvasSatin (ref) vs BC_17MSatin_pk_CanvasSatin (target)                                                                                                                                                                                                                                                                                                                  | 1e73dc6   | A3: median ΔE00 = 0.50, P95 = 1.71, R² = 0.967. D1 (rank 2): median = 0.65, P95 = 2.08, R² = 0.965. k = 13 (S1 anchors).                                                                                                                                                                                                                                                                                                                                                        | A3 wins on this pair because the substrate transform is almost pure multiplicative (same paper base, different surface finish). D1's residual stage adds noise without signal here. Expect D1 to win on substrate pairs with non-multiplicative differences.                                                                                                                                                                                                                                                                                                                                                                      | Pick a pair with strong OBA mismatch (e.g. DecorMatte vs Lyve) and re-run head-to-head. Predict: A3 still wins on the 380–410 nm bands but D1 wins in the bulk because its residual captures non-multiplicative deviation.                                                                                                                                                                                                          |
| 2026-05-22 | Ink-limit detection — Yule-Nielsen `n=2` vs linear `n=1`                     | All 27 P9000 RGB ICMs (C ramp, M ramp, Y ramp; CY/MY/CM combos where ≥ 8 patches available)                                                                                                                                                                                                                                                                                              | cc14bf6   | `n=1`: max ΔE76 = 17.8 on primary ramp (false limit at level 192/255). `n=2`: max ΔE76 = 5.3. Empirical optimum `n ≈ 2.7`: mean ΔE76 = 2.6.                                                                                                                                                                                                                                                                                                                                     | Linear Neugebauer is wrong for Epson P9000 — the optics of real ink need Yule-Nielsen. False-positive ink limits at n=1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Switch `limitsAnalyzer.computeRampErrors` to `n=2`; raise ΔE threshold from 2.0 to 6.0. Open question: should `n` be per-channel-fit instead of fixed 2?                                                                                                                                                                                                                                                                            |
| 2026-05-21 | CYNSN-2 measured-grid override + primary-extraction collapse fix             | Synthetic + sample of P9000 RGB profiles                                                                                                                                                                                                                                                                                                                                                 | f8cb1e8   | Primary extraction now returns 8 distinct corners (was collapsing onto one vertex when KNN tolerance was tight). CYNSN-2 grid now uses measured spectra at nodes within tolerance 0.08 of measured patches.                                                                                                                                                                                                                                                                     | Both fixes land but **CYNSN-2 still under-performs YNSN** on real data because the training loss does not see the measured grid — it tunes spreading for the colorant grid, then the grid is swapped post-training. Documented as Bug 2 in `docs/cynsn-pipeline.md`.                                                                                                                                                                                                                                                                                                                                                              | Fix Bug 2: pass `grid_cynsn2` into `trainCYNSN3` and use it inside the loss. Re-measure ΔE00 deltas YNSN vs CYNSN-2 on the same profile set.                                                                                                                                                                                                                                                                                        |
| 2026-05-21 | CYNSN training stability — Yule-Nielsen `n` cap                              | Sample profile during CYNSN debugging                                                                                                                                                                                                                                                                                                                                                    | 87cae9a   | Optimiser hits the `n = 10` cap on substrates where the true optimum is `n ≈ 15–20`; gradient ≈ 0 at the boundary, simplex stalls.                                                                                                                                                                                                                                                                                                                                              | Hard cap distorts model.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Raise cap to 30 or replace with a soft penalty for very large `n`. Documented as Bug 1 in `docs/cynsn-pipeline.md`.                                                                                                                                                                                                                                                                                                                 |
| 2026-05-21 | Colormath `xyzToLab` precedence fix                                          | Synthetic test pairs (ISO 11664-6)                                                                                                                                                                                                                                                                                                                                                       | a2ea011   | Pre-fix: incorrect L\* in a narrow XYZ range due to `-x/25**2` parsing as `-(x/(25**2))`. Post-fix: agreement with ISO reference within 1e-4 on Lab and within 1e-3 on ΔE00.                                                                                                                                                                                                                                                                                                    | Operator precedence bug — `**` binds tighter than unary minus. Rewritten as `-(((x/25)**2))`. ISO tests pinned.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | None — covered by `lib/colormath.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                          |
2026-06-07 | Print Mode Isolation & LOO Training | WCRW-only subset | Static D7-CAE-M0 | P95=5.29 (cross-mode), P95=3.8 (intra-mode static) | Cross-mode mixing invalid. Need per-target LOO latent fine-tuning within same print mode. Next: Dynamic Nelder-Mead on support set latents + anchor residuals (Hypothesis 14).
---

## How to add a row

1. Add a row at the top of the table with: date (ISO), short experiment name, profiles
   used, commit SHA of the analyser version, the numeric key metric (with units),
   the one-sentence conclusion, and the next concrete step.
2. If the metric needs a chart, attach the screenshot under
   `docs/experiments/<date>-<slug>.png` and reference it inline.
3. If the experiment **invalidates** a previous row, add a new row that cites the older
   commit and states the new conclusion. Do not edit history.
4. Cross-reference the relevant `H1.x` from `docs/RESEARCH_HYPOTHESIS.md` in the
   conclusion column.
